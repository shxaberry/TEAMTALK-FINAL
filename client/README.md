# TeamTalk — Client

React frontend for the TeamTalk collaborative workspace.

## Setup
npm install

## Run (dev)
npm start        # http://localhost:3000

## Build (production)
npm run build    # outputs to /build